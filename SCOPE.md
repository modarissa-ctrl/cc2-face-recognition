# Face Recognition Prototype — Scope Document

## 1. Overview

A desktop application that detects human faces in images, video files, and a live webcam feed, shows how many faces are present, and identifies detected faces by matching them against a small gallery of known people. Built as a university assignment prototype — the emphasis is on a working, demonstrable, explainable pipeline, not production robustness.

## 2. Goals

| Tier | Goal |
|------|------|
| Must | Detect face(s) in a static image and draw bounding boxes |
| Must | Report the number of faces found |
| Must | Same pipeline on video: video file playback and live webcam (selectable among multiple attached cameras) |
| Must | Recognition: match detected faces against a gallery of known-person photos, label by name |
| Must | README (build + usage) and written report |
| Stretch | Face tracking: persistent per-face IDs across video frames |
| Stretch | Liveness detection: distinguish a live person from a photo held to the camera |

## 3. Non-goals

- Training our own models (pre-trained detector/embedding models are building blocks)
- Security-grade guarantees (liveness is a demo feature, not anti-spoofing you could trust)
- GPU acceleration (CPU real-time at ~720p is the target)
- Mobile/Linux builds (Windows + macOS only)

## 4. Technology stack

- **Language:** C++17
- **Framework:** openFrameworks 0.12.x — cross-platform window, video file playback (`ofVideoPlayer`), webcam capture (`ofVideoGrabber`), drawing, GUI controls (`ofxGui`). Projects generated with oF's projectGenerator.
- **Computer vision:** OpenCV 4.x, bridged via the **ofxCv** addon
  - **Detection:** YuNet face detector (`cv::FaceDetectorYN`, built into OpenCV objdetect, small ONNX model). Fast on CPU, much more robust than Haar cascades. Also returns 5 facial landmarks per face (used for alignment and useful for stretch goals).
  - **Recognition:** SFace embeddings (`cv::FaceRecognizerSF`, built into OpenCV + ONNX model). Each face becomes a 128-d vector; matching = cosine similarity against gallery vectors with a tunable threshold.
  - Haar cascade detection kept as an optional comparison mode — a nice "classical vs. modern" section for the report.

### Stretch-goal techniques (chosen to be "own work" friendly)

- **Face tracking:** own implementation of IoU/centroid matching — associate detections between consecutive frames by bounding-box overlap, assign stable IDs, tolerate short dropouts. Small, fully explainable algorithm written from scratch.
- **Liveness detection:** blink- and mouth-movement-based. The originally considered OpenCV Facemark/LBF eyelid landmarks are not available (the bundled OpenCV ships without the `face` contrib module), so instead of a geometric Eye Aspect Ratio we measure the signals photometrically from YuNet's own landmarks: eye openness = dark-pixel fraction (iris/pupil) in a small region around each eye center, mouth openness = dark-pixel fraction (oral cavity) in a region spanning the mouth corners. A blink is a transient dip in eye openness, a yawn/talk is a transient rise in mouth darkness — each detected by a small own-written state machine with an adaptive baseline and hysteresis. A tracked face that blinks or moves its mouth within a time window is labeled LIVE; one that does neither for a full window is flagged as possibly a photo. All signal extraction, state machines, and the decision window are own code; no extra model files are needed.

### Why this satisfies the "own work" constraint

The models are building blocks (like using a JPEG decoder). Everything above them is ours and fully explainable: the frame pipeline, detection orchestration, gallery loading, embedding matching logic, threshold tuning, the tracker, the blink/liveness logic, and the entire UI. The report explains how YuNet/SFace work conceptually (CNN detector; embedding vectors + cosine similarity) without claiming we trained them.

## 5. Cross-platform & tooling strategy

One codebase; native per-platform projects generated by the oF projectGenerator:
- **macOS:** Xcode project → develop and debug in **Xcode**. OpenCV via Homebrew (or a pinned prebuilt copy).
- **Windows:** develop and debug in **VS Code** using oF's VS Code/msys2 template (C/C++ extension + gdb). Fallback if VS Code debugging proves painful: Visual Studio 2022 project (projectGenerator supports both from the same source).
- Platform quirks to expect: macOS camera-permission prompt (`NSCameraUsageDescription` in Info.plist), OpenCV linking paths on Windows.

### Python automation scripts (`scripts/`)

Small, readable Python scripts so either machine goes from clean checkout to running binary:
- `scripts/bootstrap.py` — download/verify openFrameworks + required addons (ofxCv), fetch the ONNX model files, install/locate OpenCV for the current platform, run projectGenerator.
- `scripts/build.py` — invoke the platform build (xcodebuild on macOS, make/msbuild on Windows) and place the binary + models in a runnable layout.
- Scripts are idempotent: safe to re-run, skip what's already done.

## 6. Application design (prototype level)

Single window, three input modes switchable from a small `ofxGui` panel:

```
+---------------------------------------------------+
| [video / webcam / image area with overlays]       |
|   - box per detected face                         |
|   - name label or "unknown"                       |
|   - (stretch) stable ID per face, LIVE/PHOTO flag |
|                                                   |
|  Faces: 3                                         |
+---------------------------------------------------+
| GUI panel: [Image] [Video file] [Webcam]          |
|   webcam 1/2: FaceTime HD    [Refresh webcams]    |
|   detector: YuNet / Haar     conf threshold: 0.6  |
|   match threshold: 0.36      [Load gallery]       |
|   [add face by id...]  (or press 'A' to enroll)   |
+---------------------------------------------------+
```

Core pipeline (each frame): grab frame → convert to `cv::Mat` → detect faces → align/crop each face → embed → match against gallery → *(stretch: update tracker, update blink state)* → draw boxes, labels, count.

Gallery format: a `gallery/` folder with one subfolder per person (`gallery/alice/*.jpg`); embeddings computed once at load time.

**Runtime enrollment:** faces can be added to the gallery while the app is running. With tracking on and a face visible (labeled `#id`), pressing the `A` key or clicking `add face by id...` prompts for the face's `#id` and a person name, then saves the cropped face under `gallery/<name>/`. The new photo is loaded on the next app start; the running session's recognition is intentionally not reloaded.

## 7. Milestones

1. **M0 — Skeleton (must):** oF app builds and runs on macOS (Xcode) + Windows (VS Code), OpenCV linked and version-printed; `bootstrap.py`/`build.py` work on both platforms. *(De-risks the whole build/link story first.)*
2. **M1 — Images (must):** load an image, detect faces, draw boxes, show count.
3. **M2 — Video (must):** same pipeline on video file playback and webcam, real-time; mode switching UI; count overlay.
4. **M3 — Recognition (must):** gallery loading, embeddings, matching, name labels, threshold slider.
5. **M4 — Docs & polish (must):** README with build instructions for both platforms; written report (approach, how detection/recognition work, threshold-tuning observations, results); demo assets (test images, short video, example gallery).
6. **M5 — Face tracking (stretch):** IoU/centroid tracker, stable IDs drawn on video.
7. **M6 — Liveness (stretch):** blink/mouth-movement live-photo flag on video and webcam modes; report section on its limits.

Each milestone is independently demoable. M0–M4 deliver the full required scope; M5/M6 only start once M4 is done.

### Timeline (3 weeks, deadline ~2026-07-31)

- **Week 1:** M0 + M1 — build/link story solved on both platforms, image detection working.
- **Week 2:** M2 + M3 — video/webcam pipeline and recognition complete; README drafted alongside.
- **Week 3:** M4 — report and demo assets; then M5/M6 stretch goals with whatever time remains, keeping the last 2–3 days as buffer for Windows verification and report polish.

## 8. Deliverables

- Source code (oF app, addons config, `scripts/`)
- `README.md`: build instructions for macOS and Windows, usage guide
- **Written report**: architecture, how the detector/embeddings work conceptually, own-work components (pipeline, matching, tracker, liveness logic), experiments/threshold tuning, results, limitations
- Model files (YuNet + SFace ONNX, a few MB) or fetched by `bootstrap.py`
- Sample assets: test images, a short test video, example gallery

## 9. Risks & mitigations

| Risk | Mitigation |
|------|------------|
| OpenCV linking pain inside openFrameworks, especially on Windows | M0 tackles this first, before any feature work |
| Bundled `ofxOpenCv` addon's OpenCV too old for YuNet (needs ≥ 4.5.4) | Use ofxCv + system/prebuilt OpenCV 4.x; verify in M0 |
| VS Code debugging on Windows (msys2/gdb) is flakier than Visual Studio | Fallback: generate a VS2022 project from the same code |
| Webcam permissions/quirks on macOS | Known fix (Info.plist entry); video-file mode is the fallback demo path |
| Recognition accuracy on a small gallery | Threshold slider for live tuning; multiple gallery photos per person |
| Blink/mouth-based liveness is easily fooled (video replay) / misses motionless faces | Scope it honestly in the report as a demo of the technique, not a security feature; mouth movement gives a second, independent evidence stream |

## 10. Resolved questions

1. **Timeline:** 3 weeks until the deadline (from 2026-07-10). M0–M4 committed; M5/M6 attempted in week 3 if the schedule holds.
2. **Report format:** no prescribed format — we'll use a simple structure: introduction, approach/architecture, how detection and recognition work, experiments and results, limitations, conclusions.
